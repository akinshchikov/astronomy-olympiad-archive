[project]
name = "astro-problems-archive"
version = "0.1.0"
description = "Reproducible local archive for astronomy olympiad materials"
requires-python = ">=3.12"

[tool.pyright]
typeCheckingMode = "basic"

